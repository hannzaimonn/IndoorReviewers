<section class="container">
	<table class="table table-hover table-inverse">

   	  <h2> 検索結果</h2>
  <thead>
    <tr>
     <th></th>
     <th></th>
     <th></th>
     <th colspan="3"></th>
    </tr>
  </thead>
  <tbody>
     <% @review.each do |review| %>
     <tr>
      <td>
       <%= attachment_image_tag(review, :image, :fill, 50, 50, fallback: "no_image.jpg") %>
      </td>
      <td>
      	タイトル<br>
       <%= link_to review.title, review, class: "review_#{review.id}" %>
      </td>
     </tr>
     <% end %>
  </tbody>
 </table>
</section>